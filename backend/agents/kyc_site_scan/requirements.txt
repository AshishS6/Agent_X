# KYC Site Scan v2 Dependencies

# Core dependencies (likely already installed from main project)
pydantic>=2.0.0
fastapi>=0.100.0
uvicorn>=0.23.0
httpx>=0.24.0

# Browser automation for checkout validation
playwright>=1.40.0

# Fuzzy string matching for entity matching
rapidfuzz>=3.0.0

# Async support
aiohttp>=3.8.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Note: Other dependencies like requests, beautifulsoup4, etc.
# are expected to be installed from the parent market_research_agent

